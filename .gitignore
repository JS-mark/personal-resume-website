node_modules
.DS_Store
.env
.env.local
.env.*.local

# build outputs
dist
out
.turbo
.vite
*.tsbuildinfo

# Remotion
apps/remotion/out
apps/remotion/.cache

# rendered video artifacts (generated in CI)
apps/web/public/videos/*.mp4
apps/web/public/videos/*.webm
apps/web/public/videos/*.jpg
# manifest.json 入库一个空 stub；render 会覆盖它（git 会显示 modified，
# 这是预期行为——manifest 是 generated artifact，不要把它当 source 提交）

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code per-user local settings (skills/ stays tracked)
.claude/settings.local.json

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# coverage
coverage
